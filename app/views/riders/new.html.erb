<%= content_for(:title, "Nieuwe renner") %>

<%= form_for @rider, html: { class: 'form-horizontal' } do |form| %>

  <%= render form %>

  <%= form.submit "Opslaan", :class => "btn btn-primary" %>
  <%= link_to "Annuleren", riders_path(), :class => "btn btn-default" %>

<% end %>