<table class="table table-condensed">
  <thead>
  <tr>
    <th></th>
    <th>Team</th>
    <th>Punten</th>
  </tr>
  </thead>
  <tbody>
	<% i = 0 %>
  <% @players.each do |player| %>
    <tr>
			<td><%= i += 1 %></td>
      <td><%= player.team_name %></td>
			<td><%= player.stage_points(@stage) %></td>
    </tr>
  <% end %>
  </tbody>
</table>