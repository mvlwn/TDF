<h1>Teams</h1>

<table class="table">
  <thead>
  <tr>
    <th>Naam</th>
    <th>Budget</th>
  </tr>
  </thead>
  <tbody>
  <% @teams.each do |team| %>
  <tr>
    <td><%= link_to team.name, team_path(team) %></td>
    <td><%= team.budget %></td>
  </tr>
  <% end %>
  </tbody>
</table>

<% if can? :create, Team %>
	<div class="well">
		<%= link_to "Nieuw team", new_team_path, :class => "btn" %>
	</div>
<% end %>

